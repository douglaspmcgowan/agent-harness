#!/bin/sh

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "Commit blocked: gitleaks is missing from PATH." >&2
  exit 1
fi

exec "$(command -v gitleaks)" git --pre-commit --staged --redact --no-banner
