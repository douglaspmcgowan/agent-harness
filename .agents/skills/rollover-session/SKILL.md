---
name: rollover-session
description: Deprecated compatibility alias for the cross-platform rollover workflow. Use only when a request explicitly invokes rollover-session; route execution to source-command-rollover.
---

# Rollover session compatibility alias

Read and follow `../source-command-rollover/SKILL.md`.

Preserve the canonical skill as the single implementation. Add alias-specific behavior here only when the invocation requires translation.
