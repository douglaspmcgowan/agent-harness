---
name: rollover-session
description: Deprecated Claude compatibility alias for /rollover. Use only when a Claude request explicitly invokes rollover-session; route execution to the installed rollover command.
---

# Rollover session Claude adapter

Read and follow `~/.claude/commands/rollover.md`.

Keep all workflow logic in the command. This adapter only preserves the legacy discovery name.
