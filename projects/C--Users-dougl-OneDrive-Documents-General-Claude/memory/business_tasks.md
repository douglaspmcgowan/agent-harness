---
name: business_tasks
description: "Running list of tasks Doug owes to clients/other people for his AI consulting business (Jars of Clay) — deliverables, follow-ups, things to send"
metadata:
  node_type: memory
  type: project
  originSessionId: c5cef235-881d-4903-a37f-560e8d0ce93c
---

**Jars of Clay — business tasks owed to other people.** When Doug has a task to do _for_ a client or someone else (a deliverable, a follow-up, something to send), record it here so it's not dropped. This is the client-facing counterpart to [[manual_tasks_jars_portal]] (which is Doug's own setup tasks).

**Format per item:** `- [ ] <who> — <what> — <due/where> (added YYYY-MM-DD)`

**Open as of 2026-06-13:**

- (none yet — add as they come up)

**Standing recurring tasks:**

- When a new client is created in the portal admin, send them their one-time access key + portal link (`https://<portal>/c/<slug>`). The key is shown only once at creation.

Business: **Jars of Clay** (Doug's AI consulting). Portal repo douglaspmcgowan/client-portal. See [[project_client_portal]].
